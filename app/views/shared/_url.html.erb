<%= form_for(Styles.new, url: { action: "create" }, html: { class: 'url' }) do |s| %>
  <%= s.text_field :url, placeholder: @style.url || "ENTER URL..." %>
  <%= s.submit "Collect Trash" %>
<% end %>
